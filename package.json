{
  "name": "swifttranslator-test",
  "version": "1.0.0",
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install"
  }
}
